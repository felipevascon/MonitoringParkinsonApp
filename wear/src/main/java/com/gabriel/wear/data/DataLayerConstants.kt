package com.gabriel.wear.data

object DataLayerConstants {
    // Caminho para o item de dados que contém as informações do sensor
    const val SENSOR_DATA_PATH = "/sensor_data"

    // Chaves para os valores dentro do DataMap
    const val KEY_TIMESTAMP = "timestamp"
    const val KEY_VALUES = "values"
}
